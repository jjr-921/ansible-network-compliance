- name: retrieve api_key
  hosts: palo
  gather_facts: no
  tasks:
    - name: get api key
      paloaltonetworks.panos.panos_api_key:
        provider:
          ip_address: 192.168.1.3
          username: jregis-api
          password: Welcome1234!@#$
      register: auth

    - name: show api key
      debug:
        msg: '{{ auth.api_key }}'
