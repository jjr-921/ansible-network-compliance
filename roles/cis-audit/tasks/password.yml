- name: Get password policy
  paloaltonetworks.panos.panos_op:
    cmd: "show password-complexity"
  register: password_policy

- name: Check minimum password length
  set_fact:
    password_length_ok: "{{ 'min-length 12' in password_policy.stdout }}"
